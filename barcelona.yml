environments:
  hello:
    name: hello
    image_name: quay.io/dsiawdegica/hello
    scheduled_tasks: []
    services:
      - name: web
        service_type: web
        cpu: 128
        memory: 256
        command: sh /start.sh
        listeners:
          - endpoint: hello2
            health_check_path: /
